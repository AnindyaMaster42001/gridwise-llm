{
  "public_base_url": "https://gridwise-llm-anindya3.vercel.app",
  "repository_url": "https://github.com/AnindyaMaster42001/gridwise-llm",
  "image_reference": null,
  "previous_image_reference": null,
  "video_url": null,
  "video_duration_seconds": null,
  "verified_commit": "88d75fa0de1290e4deb7b82dea2b06248d995132",
  "primary_provider": "gemini",
  "primary_model": "gemini-2.5-flash",
  "fallback_provider": "groq",
  "fallback_model": "llama-3.3-70b-versatile",
  "external_sample_report": null,
  "fresh_machine_reviewer": null,
  "_verified_locally": {
    "commit": "88d75fa0de1290e4deb7b82dea2b06248d995132",
    "tests": "301 passed with --integration, 0 failed",
    "public_samples_live_llm": "10/10 valid against organizer ground truth, 10/10 interpretation match, mean cost ratio 1.0000",
    "llm_in_path": "confirmed source=llm end to end, including from inside the container with a runtime-injected key",
    "container_check": "passed: startup 2.95s, non-root uid 10001, read-only rootfs, no secret patterns in image, healthy, clean shutdown",
    "local_image_id": "sha256:f672f19b3f666d6b2d670cba9cd7ef28574131d1eee76f8166d2c2f49519fbb9",
    "latency_local": "median 4.18s, p95 6.58s over the 10 public cases via HTTP"
  },
  "_open_blockers": {
    "provider_quota": "MEASURED 2026-09-18 against the supplied key: gemini-2.5-flash is HARD 429 on the per-DAY free-tier quota (metric generate_content_free_tier_requests, limit 20) - it did not recover after the per-minute window elapsed. gemini-3-flash-preview has its own per-model allowance and did answer, but went 429 within a few requests too. The key itself is VALID (200 from generativelanguage.googleapis.com, both configured models present in its model list). This cannot serve a judged round. Enable billing on the Google Cloud project, or supply a key from a DIFFERENT vendor.",
    "public_base_url": "DEPLOYED AND REACHABLE, but NOT yet submittable: LLM_API_KEY is not set on the Vercel project, so /optimize-energy is served by the deterministic fallback and the mandatory-LLM requirement is not met. Add the provider keys, redeploy, and confirm plan_summary no longer says 'deterministic backup interpreter'.",
    "image_reference": "image builds and passes container_check; never pushed. Needs `docker login` then push with an exact tag, and the digest recorded here.",
    "video_url": "script at deploy/VIDEO_SCRIPT.md, recording guide at deploy/VIDEO_GUIDE.md; not recorded.",
    "repo_visibility": "repository is PRIVATE, which is correct during the event. It must be made PUBLIC after the submission deadline or it cannot be evaluated.",
    "vercel_deployment_protection": "RESOLVED: the project shipped with Vercel Authentication on, which answered anonymous callers with a 302 to vercel.com/sso-api. Disabled; verified off via the API. Re-check after any project setting change.",
    "fallback_is_not_independent": "LLM_API_KEY and LLM_FALLBACK_API_KEY in the supplied env file are the SAME VALUE. Pointing the fallback at a different Gemini model does buy a separate per-model daily allowance, which is why it is kept, but it is one key on one project: a revocation, a billing stop or a ban takes both paths down together.",
    "gemini_3_unusable_output": "SUSPECTED, NOT CONFIRMED: gemini-3-flash-preview returned 'no JSON object found in the response' through our client on its one successful call. A trivial probe on that model showed thoughtsTokenCount=63, and the client sends maxOutputTokens=900 with no thinking configuration - Gemini 3 draws thinking from the same output budget, so a long prompt plus 8 few-shots can exhaust it before any JSON is emitted. The confirming diagnostic was blocked by quota. Re-run it before relying on this fallback."
  },
  "hosting_platform": "vercel (framework preset: fastapi, app/main.py at root)",
  "_fixed_in_release_prep": {
    "dockerfile_model": "the image defaulted to gemini-3.1-flash-lite, which the README's own measurement records as failing the end-exclusive hour rule ([13,14,15] for 1 PM-3 PM). Corrected to the measured gemini-2.5-flash.",
    "fallback_vendor": "four different fallbacks were configured across Dockerfile, .env.example, runtime.env.example and this manifest. Standardised on a DIFFERENT vendor (groq) so the fallback fails independently of the primary; no fallback is baked into the image.",
    "vercel_routing": "the first deployments 404'd every route. Vercel detects this project as framework 'fastapi' and serves app/main.py at the root; the api/index.py adapter was never used, and the rewrite in vercel.json replaced the received path so /health arrived as /api/index/health. The functions glob named api/index.py too, so maxDuration:60 never applied and the function ran on the 10s Hobby default against a 6.58s p95."
  },
  "_live_verification": {
    "url": "https://gridwise-llm-anindya3.vercel.app",
    "health": "200 {\"status\":\"ok\"}",
    "routing": "fixed - unknown route 404s in our error shape, malformed JSON 400s",
    "judge_local_replica": "73.00/100 measured, 20/20 cases valid, p95 0.418s, 0% failures",
    "deployment_protection": "disabled and verified anonymously",
    "env_vars": "12 set on production and preview, all hidden/secret",
    "failover_under_real_quota_exhaustion": "VERIFIED: with both Gemini models returning 429, one live request still returned HTTP 200 in 2.18s with the exact organizer-optimal cost for SAMPLE-01, via the regex backup. The service degrades rather than failing - but a request served this way does NOT satisfy the mandatory-LLM requirement.",
    "llm_in_path": "NOT YET - blocked on quota, not on configuration"
  }
}
